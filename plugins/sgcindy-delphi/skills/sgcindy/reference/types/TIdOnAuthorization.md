# TIdOnAuthorization

kind: event handler type
unit: IdHTTP

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; Authentication: TIdAuthentication; var Handled: Boolean) of object
```

