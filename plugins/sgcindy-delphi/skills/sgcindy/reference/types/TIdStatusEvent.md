# TIdStatusEvent

kind: event handler type
unit: IdComponent

A handler assigned to this event must match this signature:

```pascal
procedure(ASender: TObject; const AStatus: TIdStatus; const AStatusText: string) of object
```

