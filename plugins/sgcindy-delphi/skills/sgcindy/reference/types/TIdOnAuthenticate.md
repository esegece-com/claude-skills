# TIdOnAuthenticate

kind: event handler type
unit: IdSocksServer

A handler assigned to this event must match this signature:

```pascal
procedure(AContext: TIdSocksServerContext; var AAuthenticated: Boolean) of object
```

