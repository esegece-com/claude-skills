# TALPNSelectEvent

kind: event handler type
unit: IdSSLOpenSSL

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; aProtocols: TStringList; var aProtocol: String) of object
```

