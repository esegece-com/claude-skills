# TIdOnTLSNegotiationFailure

kind: event handler type
unit: IdExplicitTLSClientServerBase

A handler assigned to this event must match this signature:

```pascal
procedure(Asender : TObject; var VContinue : Boolean) of object
```

