# TOnNewCookieEvent

kind: event handler type
unit: IdCookieManager

A handler assigned to this event must match this signature:

```pascal
procedure(ASender: TObject; ACookie: TIdCookie; var VAccept: Boolean) of object
```

