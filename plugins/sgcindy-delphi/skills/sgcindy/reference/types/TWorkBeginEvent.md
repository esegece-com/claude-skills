# TWorkBeginEvent

kind: event handler type
unit: IdComponent

A handler assigned to this event must match this signature:

```pascal
procedure(ASender: TObject; AWorkMode: TWorkMode; AWorkCountMax: Int64) of object
```

