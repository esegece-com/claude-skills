# TLogItemStatusEvent

kind: event handler type
unit: IdLogEvent

A handler assigned to this event must match this signature:

```pascal
procedure(ASender: TComponent; const AText: string) of object
```

