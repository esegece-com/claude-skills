# TIdIRCTwoParmEvent

kind: event handler type
unit: IdIrcServer

A handler assigned to this event must match this signature:

```pascal
procedure(AContext: TIdCommand; AParm1, AParm2: string) of object
```

