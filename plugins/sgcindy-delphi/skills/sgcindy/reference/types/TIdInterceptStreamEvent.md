# TIdInterceptStreamEvent

kind: event handler type
unit: IdIntercept

A handler assigned to this event must match this signature:

```pascal
procedure(ASender: TIdConnectionIntercept; var ABuffer: TIdBytes) of object
```

