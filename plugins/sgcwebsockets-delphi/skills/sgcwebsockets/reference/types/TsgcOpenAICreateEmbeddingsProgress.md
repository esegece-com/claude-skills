# TsgcOpenAICreateEmbeddingsProgress

kind: event handler type
unit: sgcAI_OpenAI_Embeddings

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; aPosition: Int64; var aContinue: Boolean) of object
```

