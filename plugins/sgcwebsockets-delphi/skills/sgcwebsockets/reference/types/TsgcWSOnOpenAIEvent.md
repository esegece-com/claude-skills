# TsgcWSOnOpenAIEvent

kind: event handler type
unit: sgcWebSocket_API_OpenAI

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aEvent: string; var Handled: Boolean) of object
```

