# TsgcWSForexConnectEvent

kind: event handler type
unit: sgcWebSocket_API_Forex

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aSessionId: string) of object
```

