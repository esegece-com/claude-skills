# TOnWriteDataText

kind: event handler type
unit: sgcWebSocket_LoadBalancer_Client

A handler assigned to this event must match this signature:

```pascal
procedure(const aGuid, aText: String) of object
```

