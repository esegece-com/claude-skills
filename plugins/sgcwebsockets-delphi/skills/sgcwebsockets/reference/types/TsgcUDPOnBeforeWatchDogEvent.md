# TsgcUDPOnBeforeWatchDogEvent

kind: event handler type
unit: sgcUDP_Server

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; var Handled: Boolean) of object
```

