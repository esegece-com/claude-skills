# TsgcAIChatMessageEvent

kind: event handler type
unit: sgcAI_Chat

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aRole, aContent: string) of object
```

