# TIdHTTPSessionEndEvent

kind: event handler type
unit: sgcIdCustomHTTPServer

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TIdHTTPSession) of object
```

