# TsgcWSCoinbaseHeartbeatsEvent

kind: event handler type
unit: sgcWebSocket_API_Coinbase

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; aHeartbeat: string) of object
```

