# TsgcWSCexPlusErrorEvent

kind: event handler type
unit: sgcWebSocket_API_CexPlus

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; Error: String) of object
```

