# TsgcWebView2ClientCertificateRequestedEvent

kind: event handler type
unit: sgcWebView2_Classes

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aHost: string; aPort: Integer; var aHandled: Boolean) of object
```

