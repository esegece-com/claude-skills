# TsgcWSBybitDataEvent

kind: event handler type
unit: sgcWebSocket_API_Bybit

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aData: string) of object
```

