# TsgcWSCexMessageEvent

kind: event handler type
unit: sgcWebSocket_API_Cex

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; Event, Msg: String) of object
```

