# TsgcOnAuthTokenError

kind: event handler type
unit: sgcHTTP_Classes

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const Error, ErrorDescription, Data: String) of object
```

