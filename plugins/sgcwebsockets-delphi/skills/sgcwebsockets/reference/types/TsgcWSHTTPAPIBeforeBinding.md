# TsgcWSHTTPAPIBeforeBinding

kind: event handler type
unit: sgcWebSocket_Server_HTTPAPI

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const Bindings: TStrings) of object
```

