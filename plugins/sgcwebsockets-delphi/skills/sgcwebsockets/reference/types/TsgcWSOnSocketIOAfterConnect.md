# TsgcWSOnSocketIOAfterConnect

kind: event handler type
unit: sgcWebSocket_API_SocketIO

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject) of object
```

