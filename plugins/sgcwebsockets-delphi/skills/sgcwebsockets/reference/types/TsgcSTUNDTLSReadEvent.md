# TsgcSTUNDTLSReadEvent

kind: event handler type
unit: sgcP2P_STUN_Client

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aSocket: TsgcSocketConnection; const aBytes: TBytes) of object
```

