# TsgcWSHuobiErrorEvent

kind: event handler type
unit: sgcWebSocket_API_Huobi

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; Id, Code, Msg, Ts: string) of object
```

