# TsgcExceptionEvent

kind: event handler type
unit: sgcWebSocket_Classes

A handler assigned to this event must match this signature:

```pascal
procedure(Connection: TsgcWSConnection; E: Exception) of object
```

