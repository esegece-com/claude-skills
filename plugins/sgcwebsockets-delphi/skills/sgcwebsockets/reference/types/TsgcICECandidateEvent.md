# TsgcICECandidateEvent

kind: event handler type
unit: sgcP2P_ICE_Client

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aCandidate: TsgcICE_Candidate) of object
```

