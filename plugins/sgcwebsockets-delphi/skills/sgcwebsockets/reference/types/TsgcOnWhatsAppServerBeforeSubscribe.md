# TsgcOnWhatsAppServerBeforeSubscribe

kind: event handler type
unit: sgcLib_WhatsApp_Server

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aRequestInfo: TIdHTTPRequestInfo; var Accept: Boolean) of object
```

