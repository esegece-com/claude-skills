# TsgcAIChatRAGContextEvent

kind: event handler type
unit: sgcHTML_Component_AIChat

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aQuery: string; var aContext: string; var aSourceRefs: string) of object
```

