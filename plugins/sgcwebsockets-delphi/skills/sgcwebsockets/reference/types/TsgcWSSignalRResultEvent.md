# TsgcWSSignalRResultEvent

kind: event handler type
unit: sgcWebSocket_API_SignalR

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; InvocationId: String; Result: String; Error: String) of object
```

