# TsgcHTTP_JWTHeader

kind: class
unit: sgcHTTP_JWT_Classes

## Properties

| Delphi | Type |
| --- | --- |
| `alg: TsgcHTTP_JWT_Algorithm` | `TsgcHTTP_JWT_Algorithm` |
| `kid: string` | `string` |
| `typ: string` | `string` |
| `JSONText: string (read-only)` | `string` |

## Methods

```pascal
procedure Assign(aSource: TPersistent);
procedure ClearKeyValues;
procedure AddKeyValue(const aKey, aValue: string);
```

