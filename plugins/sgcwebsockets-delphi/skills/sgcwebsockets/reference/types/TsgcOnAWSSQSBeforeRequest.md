# TsgcOnAWSSQSBeforeRequest

kind: event handler type
unit: sgcHTTP_Amazon_SQS

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; var URL: String; var Handled: Boolean) of object
```

