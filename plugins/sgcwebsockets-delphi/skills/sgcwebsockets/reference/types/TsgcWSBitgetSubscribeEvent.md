# TsgcWSBitgetSubscribeEvent

kind: event handler type
unit: sgcWebSocket_API_Bitget

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; aSuccess: Boolean; const aError, aRawMessage: string) of object
```

