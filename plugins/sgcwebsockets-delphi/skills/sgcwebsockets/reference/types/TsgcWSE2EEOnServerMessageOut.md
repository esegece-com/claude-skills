# TsgcWSE2EEOnServerMessageOut

kind: event handler type
unit: sgcWebSocket_Protocol_E2EE_Server

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aText: string) of object
```

