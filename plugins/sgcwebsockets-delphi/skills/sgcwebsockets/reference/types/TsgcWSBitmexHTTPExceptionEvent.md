# TsgcWSBitmexHTTPExceptionEvent

kind: event handler type
unit: sgcWebSocket_API_Bitmex

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; E: Exception) of object
```

