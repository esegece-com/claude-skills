# TsgcWSPusherErrorEvent

kind: event handler type
unit: sgcWebSocket_API_Pusher

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; Code: Integer; Msg: String) of object
```

