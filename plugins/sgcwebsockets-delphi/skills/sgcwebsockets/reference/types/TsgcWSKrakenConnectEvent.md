# TsgcWSKrakenConnectEvent

kind: event handler type
unit: sgcWebSocket_API_Kraken

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; ConnectionId: String; Status: String; Version: String) of object
```

