# TsgcFormAIBuildEvent

kind: event handler type
unit: sgcHTML_Component_Form

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aDescription: string; var aFieldsJSON: string; var aHandled: Boolean) of object
```

