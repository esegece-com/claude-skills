# TsgcWSXTBErrorEvent

kind: event handler type
unit: sgcWebSocket_API_XTB

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aCode: string; const aDescription: String; const aRawMessage: string) of object
```

