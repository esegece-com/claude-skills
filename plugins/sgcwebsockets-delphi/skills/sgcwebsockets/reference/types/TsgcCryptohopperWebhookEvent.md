# TsgcCryptohopperWebhookEvent

kind: event handler type
unit: sgcHTTP_API_Cryptohopper

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aBody: string; const aSignature: string) of object
```

