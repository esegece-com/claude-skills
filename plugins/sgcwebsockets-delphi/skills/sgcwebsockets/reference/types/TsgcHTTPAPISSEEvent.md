# TsgcHTTPAPISSEEvent

kind: event handler type
unit: sgcHTTP_API

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aEvent, aData: string; var Cancel: Boolean) of object
```

