# TsgcWSBybitUnSubscribeEvent

kind: event handler type
unit: sgcWebSocket_API_Bybit

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aReqId: string; aSuccess: Boolean; const aError, aRawMessage: string) of object
```

