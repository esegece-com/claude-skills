# TsgcAMQP1DisconnectEvent

kind: event handler type
unit: sgcWebSocket_Protocol_AMQP1_Client

A handler assigned to this event must match this signature:

```pascal
procedure(Sender: TObject; const aCode: Integer) of object
```

